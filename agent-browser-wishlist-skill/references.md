# References

1. Agent Browser session management reference: supports `state save`, `state load`, session isolation, and reuse of authenticated state.
2. Agent Browser authentication reference: supports `--profile`, `--session-name`, explicit auth state loading, and describes 2FA as a manual intervention flow.
3. Agent Browser command reference: supports `get url`, `get title`, `wait --load networkidle`, `screenshot`, `batch`, and persistent daemon-backed command chaining.
4. Agent Browser sessions docs: warn that remote debugging exposes full browser control on localhost and should only be used on trusted machines.
5. macOS launchd is the standard local scheduler for low-frequency unattended tasks.
